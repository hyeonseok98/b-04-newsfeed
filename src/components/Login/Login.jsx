const Login = ({ togglePage }) => {
  return (
    <>
      <h3>로그인</h3>
      <input type="email" />
      <input type="password" />
      <button>ㅇㅇㅇ</button>
      <span onClick={togglePage}>회원가입 어쩌구</span>
    </>
  );
};

export default Login;
