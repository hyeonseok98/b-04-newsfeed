const Signup = ({ togglePage }) => {
  return (
    <>
      <h3>회원가입</h3>
      <input type="email" />
      <input type="password" />
      <button>ㅇㅇㅇ</button>
      <span onClick={togglePage}>로그인 어쩌구</span>
    </>
  );
};

export default Signup;
